<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="boberto_launcher_modpack_editor.MainPage">
    

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">
            <HorizontalStackLayout>
                <Picker Title="Select ModPack" x:Name="PickerModPack" ItemsSource="{Binding LocalModPacks}" SelectedItem="{Binding CurrentModPack}"/>
                <Button Text="Add" Clicked="AddModPack"/>
                <Button Text="Remove" Clicked="RemoveModPack"/>
            </HorizontalStackLayout>
            <Label
                Text="{Binding CurrentModPack.Name}"
                TextColor="White"
                FontSize="32"
                HorizontalOptions="Center" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
